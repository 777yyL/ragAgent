# dev profile 配置
spring:
  flyway:
    baseline-on-migrate: true

# 本地开发禁用 XXL-Job（避免连不上 admin 不断报错）
xxl:
  job:
    enabled: false

logging:
  level:
    com.company.knowledge: DEBUG
    org.springframework.web: INFO
