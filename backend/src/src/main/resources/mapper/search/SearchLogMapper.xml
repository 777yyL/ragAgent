<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.company.knowledge.search.mapper.SearchLogMapper">

    <!-- 热门关键词聚合：按时间窗取词频 TopN -->
    <select id="selectTrendingKeywords" resultType="map">
        SELECT query AS keyword, COUNT(*) AS cnt
        FROM search_log
        WHERE created_at >= #{start}
          AND query IS NOT NULL
          AND query &lt;&gt; ''
        GROUP BY query
        ORDER BY cnt DESC
        LIMIT #{topN}
    </select>

    <!-- 时间段检索量计数 -->
    <select id="countByDateRange" resultType="long">
        SELECT COUNT(*)
        FROM search_log
        WHERE created_at &gt;= #{start}
          AND created_at &lt; #{end}
    </select>

</mapper>
